{
  "name": "fcsifier",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Task-to-tool and tool-to-task cross-reference for CS-FSTD (EASA Opinion 01/2025).",
  "scripts": {
    "dev": "vite",
    "build": "vite build && bash scripts/check-runtime-deps.sh",
    "preview": "vite preview",
    "test": "node --import tsx --test \"test/*.test.ts\"",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "@tsconfig/svelte": "^5.0.0",
    "@types/node": "^22.0.0",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  }
}
